# Humi 微信登录无感切换热修实施计划

1. 更新 API smoke 与 H5 WebView 回归，覆盖头像保存、旧会话兜底完成身份和原生导航失败恢复。
2. 扩展身份 API 保存 `avatarKey`，增加 H5 身份兜底组件并接入 session 更新。
3. 加固 H5 到小程序的登录桥：原生页优先、旧桥回退、超时恢复。
4. 运行定向和全量发布校验，生成私有证据并提交代码。
5. 原子部署 API/H5，验证线上；完成真机验收后提交并发布微信小程序版本。
6. 针对真机反馈，将 WebP 图片迁到 API 长缓存资源出口，并把家庭邀请改成用户手势内的两阶段原生跳转，补齐 API、产品和协作回归。
